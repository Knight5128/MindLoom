---
description: MindLoom 项目改动流程：同步 README + 每次回复附带 commit message
alwaysApply: true
---

# MindLoom 改动工作流

每次对仓库做出实际改动（修改 / 新增 / 删除文件），回复中必须满足以下两条要求。

## 1. 同步更新 `README.md` 中的功能说明

如果本次改动**影响了对外可见的功能、目录结构、依赖、命令、快捷键或用户行为**，必须在同一次回复中把 `README.md` 的相关章节同步更新（功能列表、目录结构、快捷键、打包/开发命令等）。

判断是否需要更新（择其一即"需要"）：

- 新增 / 调整 / 删除了一个用户可感知的功能（如：托盘菜单、全局快捷键、新背景、新音效）
- 改变了开发或打包命令、新增/移除了运行环境要求
- 改了目录结构（`README.md` 里画了 `src/` 树）
- 改了快捷键
- 引入了用户应当知道的新依赖或新插件

**不需要更新**的典型情况（仅作提示，不必逐一对照）：

- 纯内部重构、改名、提取函数、调整代码风格
- 修 bug 但行为对用户透明
- 仅改注释、格式化、lint 修复
- 仅改图标资源、字体等纯素材替换且不引入新行为
- 仅改 CI / 构建脚本内部细节，对外命令不变

> 规则：**有疑问就更新**。宁可多写一行 changelog/feature note，也不要让 README 与实际行为漂移。

## 2. 每次回复末尾附上本次改动的 Git commit message（英文）

每次做出改动后，回复**最后**必须给出一段建议的 commit message，使用如下格式渲染（方便用户直接复制）：

````
```
<type>(<scope>): <imperative summary, <=72 chars>

- bullet 1
- bullet 2
- bullet 3
```
````

要求：

- **整段 commit message 必须使用英文**（标题与正文均英文，不夹杂中文）
- `<type>` 使用 Conventional Commits 词：`feat` / `fix` / `refactor` / `docs` / `chore` / `style` / `perf` / `build` / `ci` / `test`
- `<scope>` 可选，但当改动集中在某一模块时建议加上，如 `feat(tray):`、`docs(readme):`
- 标题行（首行）使用祈使句、首字母小写、不加句号、不超过 72 字符
- 正文以 `-` 列出 3–6 条具体改动要点，每条一句话，描述"做了什么"而非"为什么"
- **不要**实际执行 `git commit`，除非用户明确要求

## 例外

- **纯问答 / 解释 / 阅读** 类回复（没有任何文件改动）：两条都跳过
- 用户明确说"不用更新 README"或"不用 commit message"时，按用户指示

## 自检清单（每次改动结束前在心里过一遍）

- [ ] 这次改动是否影响 README 中描述的功能/命令/目录/快捷键？影响则已同步更新
- [ ] 回复末尾是否给出了本次改动的 commit message 代码块？
- [ ] commit message 的 type 是否准确？要点是否覆盖了所有实际改动的文件？
